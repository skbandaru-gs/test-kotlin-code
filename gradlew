#!/usr/bin/env sh

##############################################################################
##
##  Gradle start up script for UN*X
##
##############################################################################

# Get the absolute base directory in which "gradlew" script resides
BASEDIR=$(dirname "$0")
GRADLEW_EXEC=$(which gradle)

if [ ! -f "${BASEDIR}/gradle/wrapper/gradle-wrapper.jar"]; -- fail to Build sth
```